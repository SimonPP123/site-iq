// Empty stub for the Next.js `server-only` package, aliased in vitest.config.ts. The real package
// throws when imported outside a React Server Component (no resolvable module under vite), so server-
// only modules like src/lib/ssrf.ts can be imported and unit-tested directly.
export {};
